# uat 环境配置

server:
  port: 9091

spring:
  devtools:
    restart:
      enabled: true
      additional-exclude: /resources/**,templates/**
  thymeleaf:
    cache: false
    mode: LEGACYHTML5
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/alltypes
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
